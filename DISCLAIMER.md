# 免责声明

使用本程序前请读完。装了就视为你已理解并接受下面每一条。

## 1. 它是什么，不是什么

- 它是一个**本地**程序：接管你自己的 Codex 配置，把 Codex 的请求经由你**自己配置**的出站
  转发到官方端点，并处理响应里的 `X-Codex-Turn-State`。
- 它**不是**账号额度工具：不增加额度、不解除限流、不刷新登录、不使用任何重置手段。
- 它**不**承诺任何服务端结果。turn-state 的长度（所谓 292 / 332）只是社区总结的**经验筛选口径**，
  不是 OpenAI 公布的质量指标：符合不代表模型「满血」，不符合也不能断定降智或代理有问题。

## 2. 出站与代理

- 所有代理、订阅、机场节点都由**你自己**填入或导入；本程序不内置、不推荐、不分发任何出口，
  也不导入订阅里的 TUN / DNS / 系统规则。
- 本程序**不修改系统代理**，只影响被它接管的那个 Codex 的连接。
- 「换出口」是实验性的：它会在你配置的候选出口之间发**短探测请求**，**消耗你自己的账号额度**；
  探测次数有上限，遇到 401 / 403 / 429 停止本轮。

## 3. 它会改动你的配置

- 它会**备份并改写你真实的 `~/.codex/config.toml`**（或 `CODEX_HOME` 指向的那份），退出时恢复。
- 被强制结束、断电、崩溃时可能来不及恢复：请运行 `ccodex-sleep-state restore`，
  或在它的面板里用「检查与修复配置」。**不要从坏掉的文件里猜 API key。**
- 与 QB Gate 同用时：它与 QB Gate 的「官方 Codex 识别（turn-state）」**不能同时开** ——
  两者都要接管 Codex 的入口。QB Gate 的扩展中心会拒绝在识别线挂着时启动本程序。

## 4. 条款、合规与所在地法律

- 操纵会话状态、经由代理访问服务，**可能触碰服务商的使用条款**；某些国家或地区对代理工具
  另有法律限制。是否使用、如何使用、是否合规，**由你自行判断并承担全部后果**。
- 本程序不为规避封禁而设计，也无法达到该目的；对你账号的状态不作任何承诺。

## 5. 隐私

- 认证值与完整 turn-state 不写常规日志；本程序没有遥测，不向作者上传任何东西。
- 本地的代理配置、订阅与恢复备份可能包含敏感信息，请自行保管；求助时**不要**发送
  `auth.json`、管理口令、完整订阅链接或整个数据目录。

## 6. 没有保证

本程序按「现状」提供，不附带任何明示或暗示的保证（见 `LICENSE`）。
上游作者、本仓库维护者、QB Gate 的作者均不对使用本程序造成的任何损失负责。
